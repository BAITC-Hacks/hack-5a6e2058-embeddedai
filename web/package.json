{
  "name": "money-graph-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "check": "biome check src && tsc --noEmit",
    "format": "biome check --write src"
  },
  "dependencies": { "cytoscape": "^3.33.0" },
  "devDependencies": { "@types/cytoscape": "^3.21.9", "typescript": "^5.9.0", "vite": "^7.1.0", "@biomejs/biome": "^2.2.0" }
}
