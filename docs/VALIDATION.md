# Проверка реализации v2 — 23 сентября 2026

## Автоматические проверки

`scripts/verify.sh` завершился успешно: Ruff lint/format, mypy (11 модулей),
26 pytest-тестов, Biome, TypeScript, Vite production build, smoke собранного
frontend/API/CSV и 4 сценария Playwright в Chromium. После исправления формата
цветов сообществ повторно прошли frontend checks/build и все 4 browser-теста.

Дополнительные тесты v2 проверяют: запрет повторного использования суммы в
сопоставлении, неизвестный порядок внутри дня, обратные даты, окно 1–2 дня,
уникальность плательщиков, существование каждого ребра пути/цикла, разрыв
связности среди оставшихся вершин, точную схему CSV, чувствительность ранга,
сохранение оборота между сообществами, API-границы и неверные даты/конфигурацию.

## Официальный набор

| Показатель | Результат |
|---|---|
| Узлы | 2 248, все включены |
| Рёбра / транзакции | 3 119 / 4 840 |
| Seed / изоляты | 81 / 19 |
| Узлы на границе | 444; не объявлены terminal по отсутствию выхода |
| Сообщества / компоненты | 88 / 35 (16 неодноузловых + 19 изолятов) |
| Топ приоритетов | 50 строк |
| Чтение и анализ, прогон v2 | 1,149 секунды; установка зависимостей и экспорт не входят |
| Полный Parquet → анализ → запись CSV и JSON, отдельный прогон | 1,239 секунды |
| Минимальное совпадение TOP 20 при изменении одного веса ±20% | 90%, 12 сценариев |
| Узлы с положительным сопоставлением входа/выхода через 1–2 дня | 297; не число доказанных транзитных счетов |

Распределение основных ролей: coordinator 30, distributor 38, transit 68,
consolidator 75, terminal 1 034, peripheral 1 003. Правила назначения ролей
сохранены; изменены приоритет, объяснения и дополнительные наблюдения.
Это результат эвристики, не проверенная истинная разметка.
Параметры и SHA-256 источников — `artifacts/run_report.json`.

## Статический эксперимент: удалить 5 активных узлов

| Стратегия | Потеря связности оставшихся пар | Затронутый наблюдаемый оборот | Крупнейшая компонента |
|---|---:|---:|---:|
| Приоритет v2 | 25,04% | 14,61% | 1615 |
| Число связей (degree) | 23,33% | 13,72% | 1657 |
| Случайный выбор, среднее 20 прогонов | 0,30% | 0,24% | 1869,65 |

До удаления крупнейшая компонента — 1877 узлов. Эксперимент занял 0,67 с локально.
Случайный разброс потери связности: 0–1,77%. Эти цифры характеризуют один граф,
одно N и одну версию правил; они не устанавливают AML-точность или превосходство
над конкурентами. Не являются прогнозом остановленных денег. Формула в METHODOLOGY.

## Браузер

Воспроизводимые тесты проверяют:

- полный int64 gid без округления, изолированный seed, неизвестный gid и восстановление;
- доказательную карточку, временную диаграмму, скачивание справки с SHA-256;
- 12 сценариев чувствительности, удаление узлов и сравнение трёх стратегий;
- обзор сообществ, переход к кластеру, скачивание настоящего PNG;
- загрузку трёх файлов, CSV точной схемы, сохранение run после reload;
- ширину 390 px без горизонтального переполнения, включая результаты симуляции.

Отдельно открыт официальный набор в production-сборке на localhost:3011:
все 2248 узлов, 88 сообществ, доказательная карточка первого приоритета,
пути от пяти из шести seed-предков, 55 сочетаний повторяемых звеньев.
JavaScript-ошибок нет. Скриншоты просмотрены; исправлены серые цвета сообществ,
которые не выявлялись прежними проверками только HTTP/JavaScript.

## Ограничения проверки

Нет истинных меток ролей; accuracy не измеряется. Чувствительность ограничена
весами при фиксированных данных, порогах ролей и Louvain. LLM не включён.
Публичный набор синтетический; исходный официальный архив не публикуется.


## Обновлённый VPS

Версия приложения 0.2.0, правила 2.0. Через публичный HTTPS проверены: синтетическая
маркировка, цветной обзор сообществ, чувствительность/симуляция, доказательная
карточка, загрузка синтетических Parquet, CSV на 80 узлов, сохранение run после
reload и мобильная ширина. HTTP 200, health 0.2.0, ошибок JavaScript и неверных
цветов нет. Сервисы money-graph и Caddy активны. Предыдущий релиз сохранён для
отката; runtime-каталог не заменялся. Официальный архив на VPS не переносился.

В отслеживаемых файлах не найдены шаблоны API-ключей/приватных ключей; исходные
private/ и data/ не включены в релиз. Сканирование шаблонов не является полным
аудитом безопасности.
