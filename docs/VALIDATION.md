# Проверка реализации — 23 сентября 2026

## Автоматические проверки

- `scripts/verify.sh` завершился успешно: Ruff lint/format, mypy, 17 pytest-тестов,
  Biome, TypeScript, Vite production build, smoke собранного frontend и API.
- После дополнительной защиты CLI от перезаписи входного каталога повторно прошли
  Ruff, mypy и все 17 тестов. Эта защита не меняет вычислительный алгоритм.
- Создан отдельный чистый checkout из Git с новым Python-окружением: установка
  по lockfile, генерация синтетического набора, CLI-расчёт и три CSV выполнены.
- В проверяемых tracked-файлах не обнаружены шаблоны API-токенов и приватных ключей;
  каталоги private/ и data/ не отслеживаются Git.

## Официальный набор

| Показатель | Результат |
|---|---|
| Узлы | 2 248, все включены |
| Рёбра / транзакции | 3 119 / 4 840 |
| Seed / изоляты | 81 / 19 |
| Узлы на границе | 444, не классифицированы terminal по отсутствию выхода |
| Сообщества, включая изолированные | 88 |
| Топ приоритетов | 50 строк |
| Итоговый вычислительный прогон | 1,58 секунды; время чтения/анализа, не установка зависимостей |
| Browser upload-прогон | 0,94 секунды вычислений; 2 248 строк в скачанном CSV |

Распределение ролей: coordinator 30, distributor 38, transit 68,
consolidator 75, terminal 1 034, peripheral 1 003.
Это результат правил v1, не проверенная истинная разметка.
Подробные параметры и SHA-256 входа — `artifacts/run_report.json`.

## Проверка в Chrome

- Открытие production-сборки на localhost:3010, отображение показателей и графа.
- Поиск полного 18-значного gid без округления и открытие карточки.
- Просмотр всех 88 сообществ.
- Загрузка трёх официальных файлов через форму и успешный повторный расчёт.
- Понятная ошибка отсутствующего gid.
- Поиск изолированного seed и узла depth=4, правильные пояснения ограничений.
- CSV содержит 2 248 строк, обновление страницы восстанавливает загруженный run.
- Отсутствуют ошибки JavaScript; при ширине 390 px нет горизонтального переполнения.

## VPS

- https://cca8f159.nip.io/ возвращает 200 по доверенному HTTPS.
- `/health`, bootstrap, поиск узла, три CSV доступны через публичный адрес.
- В Chrome проверены синтетическая маркировка, загрузка синтетических файлов,
  поиск узла, 80 строк CSV; ошибок JavaScript нет.
- systemd-служба активна и включена; перезапуск службы сохраняет результаты.
- Публичный начальный набор синтетический; официальный архив на VPS не публиковался.
- Caddy ограничивает размер тела запроса до 32 МБ, API — каждый файл до 10 МБ.

## Практические пределы

LLM-интеграция не включена; дополнительная функция — объяснимые рекомендации
по следующей проверке и отображение ограничений наблюдения. Нет обученной AML-модели,
калиброванной вероятности или проверки accuracy. Формулы, границы данных и
масштабирование описаны в README и METHODOLOGY.md.
