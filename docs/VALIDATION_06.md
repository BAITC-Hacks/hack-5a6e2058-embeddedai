# Проверка релиза 0.6.0 · 23 сентября 2026

Правила 2.2, Python 3.12, Linux, Intel i5‑8250U. Здесь указаны выполненные
проверки, а не обещание одинакового времени на других машинах.

## Пайплайн и артефакты

Каноническая команда с официальными 3 Parquet выполнена успешно. Все 2248 узлов,
88 сообществ и TOP 50 сохранены; новый `report.html` создаётся тем же запуском.
Время `total_runtime_seconds` — **5.23 с**, из них аналитика **4.598 с**.
Повтор с `--offline --frozen --no-dev` в `artifacts/` — **5.078 с**.
HTML официального набора — **1 924 236 байт**, генерируется локально и не
публикуется на общем VPS.

CSV побайтно совпадают с предыдущим релизом:

| Файл | SHA-256 |
|---|---|
| nodes_roles.csv | `ab8677382abfdedcfbd429b404be38729702cee3d794e29098aeb8787492c127` |
| clusters.csv | `73c06950a8023a971dbeee1ad30e0d7a297692730902b4efa489ea4b74b8dc20` |
| top_nodes.csv | `2450394e05c4309ec3b1ea06b5bafe00ac5f140b8224c55972a6ce5c23c6497d` |

## Автономный отчёт и диагностика

Chromium открыл `file://.../report.html` при блокировке всех сетевых запросов:
поиск полного gid, фильтр роли, сортировка, переход по ссылке на скрытую строку,
светлая/тёмная тема с сохранением, пустая выдача и ширина 390 px проверены.
Без JavaScript остаётся статическая таблица. Внешних запросов и ошибок консоли
не было. Генерация отдельного синтетического HTML для 10 000 строк — 0.205 с,
примерно 10 МБ; это замер рендеринга отчёта, не всего аналитического пайплайна.

Роли: 16 сценариев, минимум сохранённых назначений **94.48%** среди 2229 активных
узлов. Сообщества: восемь сценариев, минимум ARI **0.7960**, только смена seed —
минимум **0.8780**. 19 изолятов исключены из долей согласия. Диагностика отдельно
заняла **1.913 с**; исходный результат не изменился. [Подробности](ROBUSTNESS.md).

## Реальные обращения к OpenAI

Использован собственный серверный ключ, модель `gpt-4.1-mini-2025-04-14`.
Обычные автоматические тесты не используют этот ключ и не делают платных вызовов.
Три настоящих запроса на синтетическом графе:

| Вопрос | Наблюдаемый результат | Время / токены input+output |
|---|---|---|
| Кто собирает деньги с этих пятерых? | Три общих прямых получателя; 550000, 350000, 150000 KZT от выбранных пяти источников; по пять операций | 3.325 с / 913 |
| Пять консолидаторов с максимальной входящей суммой | В графе один подходящий узел: 210000 KZT; остальные четыре не выдуманы | 2.494 с / 857 |
| Есть ли циклы у выбранного узла? | Ноль найденных коротких циклов у указанного узла; ограничение поиска показано | 3.926 с / 849 |

Суммы, направления, число источников и ссылки проверены детерминированным
исполнителем по рёбрам графа. В API передавались вопрос и строковые gid,
без таблиц и заметок. Дополнительная первая проверка вызова функции потребовала
840 токенов. После этих измерений промпт дополнен правилами сохранения выбранной
группы и допустимых сочетаний фильтров; эти ограничения проверяются сервером.

## Скорость API

Отдельный TestClient-прогон официального набора: первый разбор/проверка сохранённого
JSON — 0.6053 с; после кеширования TOP — 0.0076 с, сообщества — 0.0063 с,
граф — 0.0222 с, отфильтрованная очередь — 0.0159 с. Повторная диагностика
устойчивости из кеша — 0.0082 с. Это локальные измерения без сетевой задержки.
При перезапуске проверенный начальный расчёт также сохраняется в кеше.
Изменение, замена и удаление JSON инвалидируют кеш; повреждённые/устаревшие
данные не выдаются за актуальные, что проверено отдельными тестами.

## Интерфейс и полный quality gate

`uv run --frozen python scripts/verify.py` завершился с кодом 0:

- Ruff lint/format, mypy, TypeScript и Biome прошли;
- **322 Python-теста** прошли за 29.85 с;
- production build и smoke frontend/API/CSV прошли;
- **28 Playwright-сценариев** прошли за 49.6 с.

После изоляции тестового окружения от унаследованных AI-ключей все 322 Python-теста
дополнительно прошли при специально заданных фиктивных `OPENAI_API_KEY` и коде
доступа (30.36 с). Есть предупреждения библиотек: pandas на крайних int64,
устаревание связки Starlette/httpx и fallback Chromium для текущего Linux;
ошибок тестов нет. На Windows/macOS этот прогон не выполнялся.

На официальном наборе три новых контекста браузера с прогретым кешем сервера:

| Показатель | Прогон 1 | Прогон 2 | Прогон 3 |
|---|---|---|---|
| Карточка первого узла готова | 442 мс | 333 мс | 333 мс |
| Карта 350 узлов готова | 5203 мс | 5331 мс | 5064 мс |
| Максимальная блокировка основного потока | 299 мс | 334 мс | 302 мс |

Ранее измеренная блокировка COSE в версии 0.5 составляла 4567 мс. Теперь COSE
работает в Web Worker, поэтому карточка и управление доступны до завершения
карты. Это улучшение отзывчивости, не обещание десятикратного ускорения самой
раскладки. Запасная сетка не использовалась, ошибок консоли не было.
Начальный JS ~71 КБ, Cytoscape ~443 КБ загружается отдельным chunk.
После добавления пункта скачивания HTML повторно прошли frontend check/build.

Проверены сохранение очереди/заметок, безопасный CSV, отсутствие потери точности
gid, ошибки и отмена AI, смена набора при ожидающем ответе, экранирование,
ссылки, отсутствие ключа, устойчивость, отмена worker и восстановление после
ошибки загрузки chunk. Существующие сценарии поиска, тем, загрузки, расследования
и PNG также прошли.

## Публичный и локальный запуск

VPS обновлён до 0.6.0; `/health` проверен локально на сервере и по доверенному
HTTPS. Службы money-graph и Caddy активны. Публичный начальный набор остался
синтетическим: 80 узлов, 95 рёбер. Официальные Parquet и private/ в релиз
не включались. Ключ размещён отдельно вне web-root; платный endpoint защищён
кодом доступа, который не входит в JS или Git.

Все **28 браузерных сценариев** дополнительно прошли на
`https://cca8f159.nip.io` за 1.1 минуты. После добавления пункта HTML выполнен
отдельный реальный браузерный сценарий без подмены API:

1. Выбраны пять синтетических seed в очереди.
2. Отправлен вопрос «Кто собирает деньги с этих пятерых?» с кодом доступа.
3. HTTP 200 за **3.627 с**, **1075 токенов**; все пять gid сохранены в плане.
4. Найдены три получателя с прямыми суммами 550000, 350000 и 150000 KZT.
5. Переход по ссылке открыл правильную карточку; граф стал доступен для PNG.
6. Скачан `report.html` (119400 байт) с явной маркировкой синтетики.

Ошибок JavaScript не было. Снимки в `docs/images/` сделаны на синтетике,
поле кода доступа перед снимком очищено.

После финальной правки автоматической прокрутки ответа/ошибки frontend check/build
и пять целевых AI-сценариев прошли (11.1 с). Повторён тот же настоящий публичный
сценарий: HTTP 200, **1.509 с, 1075 токенов**, те же три получателя и суммы,
ссылка и скачивание HTML работают, ошибок JavaScript нет. Без кода доступа
публичный AI возвращает 401; прямые запросы к private/ и result.json — 404.
Финальный снимок видимого ответа сделан ещё одним успешным повтором того же
запроса: 3.056 с, 1075 токенов, те же факты. Всего ручные проверки этого релиза
использовали восемь обращений и 7691 токен input+output.

На локальном официальном наборе запрос «три консолидатора с самой большой
входящей суммой» вернул тот же порядок и gid, что API очереди с соответствующим
фильтром и сортировкой: **2.965 с, 1007 токенов**. Модель получила только вопрос,
официальные таблицы ей не передавались.

Проверка публикуемых файлов не обнаружила переданный OpenAI-ключ или код доступа.
`private/` не отслеживается Git. В основном пути нет TODO/FIXME или фиктивных
ответов; совпадения `placeholder` относятся к подсказкам полей интерфейса.
